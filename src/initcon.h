#include<vector>

#include "element.h"
#include "node.h"
#include "enumtypes.h"

using namespace std;

extern int initcon(vector<node> Nodes, vector<element>& Elements, testtypedescr testtype, double it);
