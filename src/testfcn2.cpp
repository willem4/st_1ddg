#include "testfcn2.h"
#include <math.h>
double testfcn2(double x)
{
  double f; 
  //f = x*(x-3)/2.25;
  //f = -f*f*f;
  f = 3.11;
  //f = cos(x);
  //double pi = 4.0*atan(1.0);
  //f = cos(2*pi*x/3);
  return f;
}
